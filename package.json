{
  "name": "gsd-auto-continue",
  "version": "1.0.0",
  "description": "GSD Auto Continue Extension - two-tier recovery for hot-context retries and root-cause fix loops",
  "gsd": {
    "extension": true
  },
  "scripts": {
    "test": "node --test index.test.mjs",
    "verify:boundaries": "node scripts/verify-boundaries.mjs",
    "verify": "npm run test && npm run verify:boundaries"
  },
  "pi": {
    "extensions": [
      "index.ts"
    ]
  }
}
