{
  "name": "gsd-auto-continue",
  "version": "1.0.0",
  "type": "module",
  "description": "Small Pi/GSD extension that retries recoverable auto-mode stops.",
  "gsd": {
    "extension": true
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run test"
  },
  "pi": {
    "extensions": [
      "index.ts"
    ]
  }
}
